---
all:
  children:
    # The machine that already runs the k3s server (control plane).
    control_plane:
      hosts:
        pc:
    # The Raspberry Pi, which joins the cluster as a worker (k3s agent).
    raspberry:
      hosts:
        rpi5:
