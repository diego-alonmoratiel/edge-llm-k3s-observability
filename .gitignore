# Ansible
*.retry
.ansible/
raspberry/ansible/tmp/

# Secrets (never commit)
raspberry/ansible/inventory/group_vars/all/vault.yml
raspberry/ansible/vault/vault-password

# Kubeconfig
kubeconfig
*.kubeconfig

# Personal study notes (kept out of the repo)
raspberry/docs/ANNOTATED-FLOW.md
